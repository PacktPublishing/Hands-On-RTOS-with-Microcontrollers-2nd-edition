# Required packages for colorSelector.py
# * Use: pip install -r requirements.txt
# * Be careful not to replace installed-packages that are required, 
#   e.g., replacing an older version that is still needed.
astroid
colorama
isort
lazy-object-proxy
mccabe
pylint
pyserial
PySimpleGUI
pythoncrc
PyYAML
six
wrapt